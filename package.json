{
  "name": "polycoachsepolia",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph codegen && graph build",
    "auth": "graph auth --studio 96bcced04a4236a43d507f2561dde45b",
    "deploy": "graph deploy --studio hellopaypolygon",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.80.0",
    "@graphprotocol/graph-ts": "^0.35.1",
    "matchstick-as": "^0.6.0"
  }
}
